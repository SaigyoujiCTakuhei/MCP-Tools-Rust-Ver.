# 长期记忆功能方案（PostgreSQL + pgvector）— 供审核

> 状态：**P1 已实施并验证**（2026-08-31）；本文档保留设计原文与决策记录
> 日期：2026-08-30
> 前置实测：本机 PostgreSQL 17.11 **运行中**，pgvector 0.8.0 **已安装**（支持 HNSW 索引）

---

## 0. 术语确认

「大象数据库」= **PostgreSQL**（吉祥物为大象 Slonik）；「包含向量数据库」= **pgvector** 扩展
（提供 `vector` 类型 + HNSW/IVFFlat 向量索引）。若你指的是其他产品请纠正。

## 1. 目标与非目标

**目标**：为 MCP 服务器增加跨会话长期记忆——工具可写入带语义向量的记忆条目（事实/偏好/事件），
并按相似度检索；数据全部存本地 PostgreSQL。

**非目标（本期不做）**：多用户与权限体系、对话→事实的自动提炼（P3）、跨机同步、遗忘曲线自动衰减（P3）。

## 2. 架构选型

- **形态**：`plugins/memory/` 域插件（与 pdf_reader / sequential_thinking 同构），服务器零改动。
- 子进程模型下每次调用独立 → **每次一条 PG 连接**（本地连接 1~3ms，可接受；不引入连接池复杂度）。
- **嵌入（Embedding）抽象**：`Embedder` trait，两档实现，配置切换：

| 档位 | 实现 | 模型 | 维度 | 特点 |
|------|------|------|------|------|
| `local` | fastembed-rs（ONNX CPU 推理） | bge-small-zh-v1.5 | 512 | 离线可用；首次运行下载模型 ~100MB（`models/`，gitignore）；Pi 上单条 ~100-300ms |
| `api` | reqwest 调 OpenAI 兼容 `/embeddings` | 按配置 | 1536 等 | 质量上限高；依赖网络与 API key |

- 向量维度是**表结构级决策**（`vector(N)` 列）：换模型 = 跑迁移脚本对全表重嵌入。初始化 SQL 按 config 的 `dims` 参数化生成。

## 3. 数据库 Schema（`plugins/memory/migrations/001_init.sql`）

```sql
CREATE EXTENSION IF NOT EXISTS vector;

CREATE TABLE IF NOT EXISTS memories (
    id         BIGSERIAL PRIMARY KEY,
    scope      TEXT        NOT NULL DEFAULT 'default',  -- 命名空间：项目/会话
    kind       TEXT        NOT NULL DEFAULT 'fact',     -- fact | preference | episode | note
    content    TEXT        NOT NULL,                    -- 记忆原文
    embedding  vector(512) NOT NULL,                    -- 维度随 embedder 配置
    metadata   JSONB       NOT NULL DEFAULT '{}',       -- 自由扩展（来源、标签等）
    importance REAL        NOT NULL DEFAULT 0.5,        -- 0.0~1.0，检索排序权重
    source     TEXT        NOT NULL DEFAULT 'manual',   -- manual | auto
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS memories_hnsw
    ON memories USING hnsw (embedding vector_cosine_ops);
CREATE INDEX IF NOT EXISTS memories_scope
    ON memories (scope, updated_at DESC);
```

首次运行任意 memory 工具时自动执行该 SQL（幂等），无需手工初始化。

## 4. 工具面（4 个 `kzm-*` 工具，对应 v11 Lib/memory 的职能）

| 工具 | 参数 | 行为 | 注解 |
|------|------|------|------|
| `kzm-memory-save` | `content`(必填), `scope?`, `kind?`, `importance?`, `metadata?` | 嵌入 + 插入 → `{id}` | writes |
| `kzm-memory-search` | `query`(必填), `scope?`, `limit?=5`, `min_score?=0.0` | 嵌入查询 → HNSW 余弦 top-K → `[{id, score, content, kind, metadata, created_at}]` | read_only |
| `kzm-memory-list` | `scope?`, `limit?=20`, `offset?=0` | 按时间倒序浏览 | read_only |
| `kzm-memory-delete` | `id` 或 `scope`+`all=true` | 删除（支持批量清空命名空间） | destructive |

检索 SQL 形态：`SELECT id, content, kind, metadata, 1 - (embedding <=> $1) AS score
FROM memories WHERE scope = $2 ORDER BY embedding <=> $1 LIMIT $3;`

## 5. 配置（config.yaml 新增节）

```yaml
memory:
  dsn_env: "KZM_MEMORY_DSN"   # 环境变量名 → postgres://kzm:***@127.0.0.1:5432/kzm_memory（密码不落盘）
  embedder: "local"            # local | api
  model: "bge-small-zh-v1.5"
  dims: 512
  api_base: ""                 # embedder=api 时的 OpenAI 兼容端点
  api_key_env: "EMBED_API_KEY" # embedder=api 时的 key 环境变量名
```

## 6. 依赖与体积

- PG 客户端：`postgres` crate（**同步**——子进程内同步无碍，依赖树小）；不用 sqlx/tokio 全家桶。
- `local` 档：`fastembed-rs`（拉 ONNX Runtime，编译较重）+ 模型文件；`api` 档：仅 reqwest。
- 逐档编译：`plugins/memory/Cargo.toml` 用 feature（`local-embed` / `api-embed`）控制，默认 `local-embed`。

## 7. 安全

- DSN 密码只走环境变量；仓库与日志不落明文。
- PG 仅监听本机；scope 隔离；delete 工具标 destructive。
- 工具入参仍走服务器侧 JSON Schema 校验（inputSchema 声明到工具定义里）。

## 8. 分期

- **P1（本期）**：save / search / list / delete + local embedder + 自动初始化 SQL。
- **P2**：api embedder、混合检索（pg 全文 tsvector + 向量 RRF 融合）、写入去重（相似度 >0.98 拒绝或合并）。
- **P3**：自动记忆提炼（对话→事实抽取，依赖 LLM 工具链）、importance 时间衰减、HNSW 参数调优。

## 9. 数据归属澄清（2026-08-31 修正）

`memory_chunks` 表**不是 v11 的资产**：它是 **dsh 记忆插件（HY3 会话）**建设的 PG 存储
（import_memories_to_pg.py 导入 Hermes 记忆/日志/反思），v11 的 memory 模块走的是
Markdown + ChromaDB 的另一套。本插件复用的是这张表（172 条存量）。
原 Python 导入栈（embed-venv + onnxruntime）随设备退役，其批量导入职能由
`kzm-memory-import` 承接（分块/来源规则 1:1 移植，幂等语义由 DROP 重建改为增量跳过）。

## 9.1 与 v11 的关系

v11 `Lib/memory` 的 Markdown+ChromaDB 形态不迁移（ChromaDB 已弃用，数据已在 8/28
经 bge 向量化进 PG）。v11 的 35 个提示词已迁至 mcp_data/prompts。

## 10. 请审核的决策点

1. **embedder 档位**：`local`（离线、512 维、Pi 推理 100-300ms/条）还是 `api`（走你的模型配置）还是两档都实现、配置切换？（建议：两档都要，默认 local）
2. **向量维度**：定 512（bge-small-zh-v1.5）？（建表后改动需重嵌入）
3. **scope 粒度**：按项目/按会话/单一全局？（建议：默认 `default`，工具参数可传）
4. **建库**：PG 已运行但还没有 `kzm_memory` 库与角色——需要 `sudo -u postgres` 建库建角色
   （或给我一条 sudo 密码授权方式）；库名/角色名用 `kzm_memory` / `kzm` 可以吗？
5. **本地嵌入模型下载**：fastembed 首次运行要从 HuggingFace 拉模型（~100MB）——
   若网络需代理，届时用 `proxychains4` 跑一次初始化，或你手动下载模型放到 `models/`。
