# KazeMiMiRin MCP Server (Rust) — 初步技术规格文档

> **⚠️ 本文档已过时（2026-08-30）**：下文第 6~11 节描述的 SSE Transport、`2024-11-05` 协议版本、
> 「MCP 2.0 迁移清单」等内容已完成改造或被修正，实际实现以 [README.md](./README.md) 与
> [MCP_2.0_合规与双端兼容审查报告_2026-08-30.md](./MCP_2.0_合规与双端兼容审查报告_2026-08-30.md) 为准。
> 现状速记：单一现代端点 `POST /mcp`（2026-07-28）+ Legacy `/sse`、`/message`（2024-11-05，llama.cpp UI）；
> 无握手（现代面）/ initialize（legacy 面）；`_meta` 用 `io.modelcontextprotocol/*` 前缀键；
> 错误码 -32020/-32022/-32602；结果恒带 resultType 与 result._meta.serverInfo。
>
> 状态：初步草案（Preliminary Draft）
> 对应代码版本：`New_Architecture_v00`（截至 2026-08-27 工作区快照）
> 作者：风见血月
> 文档目的：记录当前**已实现**的架构与行为，作为后续 2.0 迁移的基线。

---

## 1. 概述

本项目是 `KazeMiMiRin` MCP 服务器的 **Rust 纯二进制实现**。从 `mimoclaw_workspace/mcp-dashboard`（800 行单文件原型）演进而来，保留其 WebUI 与热插拔能力，并重构为完全模块化架构。设计目标是作为通用工具集 MCP 服务器，供 `llama.cpp` 的 WebUI 经浏览器直连集成。

### 1.1 协议合规声明（重要）

严格**对齐 MCP 协议版本 2.0 / 2026-07-28 规范**，以 MCP 协议版本 2.0 为基础开发。

---

## 2. 系统架构

### 2.1 模块划分

```
src/
├── main.rs                 入口：装配 config / registry / logs / 路由，启动 axum 服务
├── config.rs              配置加载（config.yaml）
├── UI/
├── core/
└── tools/                 23 个工具实现，按领域分模块
    ├── tool 1/
    ├── tool 2/
    ├── tool 3/
```

### 2.2 共享状态（`AppState`）

```rust
pub struct AppState {
    pub registry: Arc<ToolRegistry>,        // 工具注册中心
    pub logs: Arc<LogSystem>,               // 日志系统
    pub response_tx: broadcast::Sender<serde_json::Value>, // SSE 响应推送通道
}
```

请求处理链：`main.rs` 构建 `AppState` → 注入三个子路由（SSE、MCP POST、Dashboard API）与 WebUI 根路由 → 顶层叠加全局 `CorsLayer`（允许任意 Origin，便于 llama.cpp UI 浏览器直连 SSE）。

---

## 3. 技术栈与依赖

| 依赖 | 版本/约束 | 用途 |
|------|-----------|------|
| `rmcp` | `0.4` | 声明依赖（注：协议层当前为手写，未使用其服务端实现，见 §9.3） |
| `tokio` | `1`（full） | 异步运行时 |
| `axum` | `0.7`（`ws`, `macros`） | HTTP 框架 + 路由 |
| `tokio-stream` | `0.1` | 流处理 |
| `fs-err` | `3`（tokio） | 文件操作错误增强 |
| `uuid` | — | 会话标识生成 |
| `tower-http` `CorsLayer` | — | CORS 中间件 |
| `tracing` / `tracing-subscriber` | — | 结构化日志 |
| `webbrowser` | — | 启动时自动打开浏览器 |

> 注：`Cargo.toml` 显示 UUID 用途标注为「MCP 会话标识」——对应 SSE 传输的 `sessionId`。

---

## 4. 配置（`config.yaml`）

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `server.host` | string | `"127.0.0.1"` | 监听地址 |
| `server.port` | int | `58081` | 监听端口 |
| `server.auto_open_browser` | bool | `true` | 启动后自动打开系统默认浏览器 |
| `logging.level` | string | `"info"` | trace/debug/info/warn/error |
| `logging.format` | string | `"json"` | json/pretty |
| `tools.discovery_path` | string | `"src/tools"` | 工具目录（**当前未启用动态发现**，工具于编译期在 `main.rs::register_all_tools` 静态注册，见 §6 注释） |
| `tools.default_timeout` | int | `30` | 工具调用默认超时（秒） |

---

## 5. 工具注册中心（`ToolRegistry`）

### 5.1 数据结构

- `ToolEntry`：单条工具记录（含 `ToolDefinition` 元数据 + 执行器 + 启用开关）。
- `ToolDefinition`：名称、描述、输入 JSON Schema、来源标识（`source`）。
- `ToolChangeEvent`（枚举，经 `broadcast` 广播）：
  - `Registered { name }` — 新工具注册
  - `Toggled { name, enabled }` — 启用状态切换

### 5.2 公开方法

| 方法 | 说明 |
|------|------|
| `new()` | 构造空注册中心，初始化广播通道 |
| `notify_rx()` | 返回 `ToolChangeEvent` 订阅者（供 SSE 监听工具变更） |
| `register(name, desc, schema, source, executor)` | 注册工具并向 `notify_tx` 发送 `Registered` |
| `get(name)` | 按名取 `Arc<ToolEntry>` |
| `list()` | 返回启用中的工具定义列表（`tools/list` 用） |
| `list_all()` | 返回全部工具定义（Dashboard 用，含禁用项） |
| `toggle(name)` | 切换启用状态并广播 `Toggled` |
| `count()` | 当前工具总数 |

### 5.3 热插拔机制

`toggle` 改变工具的启用位，并经 `broadcast` 通知所有订阅方；`sse.rs` 监听该事件后主动推送 `notifications/tools/list_changed`，使已连接客户端刷新工具清单。**注意**：热插拔仅切换「是否出现在 `tools/list` 与是否可被 `tools/call` 调用」，不卸载已编译代码（工具在进程启动期已全部静态注册）。

---

## 6. 工具集（23 个）

按模块归类，全部经 `main.rs::register_all_tools` 静态注册：

| 模块 | 工具 | 数量 |
|------|------|------|
| `add` | `add` | 1 |
| `file_ops` | `read_file`, `write_file`, `delete_file`, `file_info` | 4 |
| `directory_ops` | `list_directory`, `create_directory` | 2 |
| `system` | `get_system_info`, `get_current_date`, `get_time` | 3 |
| `shell` | `run_command` | 1 |
| `net` | `web_fetch`, `web_search`, `download_file` | 3 |
| `git` | `git_clone`, `git_pull` | 2 |
| `search` | `grep`, `search_files`, `complex_search` | 3 |
| `text` | `reverse_text`, `hello_world` | 2 |
| `metrics` | `count_lines`, `calculate` | 2 |
| **合计** | | **23** |

每个工具由 `ToolDefinition`（元数据 + JSON Schema）+ `Box<dyn ToolExecutor>`（执行器）组成，统一经 `register_tool()` 注入 `ToolRegistry`。

---

## 7. MCP 传输层

### 7.1 JSON-RPC 方法实现

| 方法 | 处理位置 | 说明 |
|------|----------|------|
| `initialize` | handler.rs:102 | 返回 protocolVersion=`2024-11-05`、capabilities=`{tools:{listChanged:true}}`、serverInfo=`KazeMiMiRin MCP Server 0.1.0` |
| `notifications/initialized` | handler.rs:122 | 握手完成通知（无响应体） |
| `tools/list` | handler.rs:128 | 返回 `registry.list()` |
| `tools/call` | handler.rs:148 | 按名取执行器并调用 |
| `ping` | handler.rs:214 | 心跳（**2.0 已删除**） |

> 未实现 `resources/*` / `prompts/*` 方法——本服务器为 **tools-only**。

### 7.2 传输方式

1. **HTTP+SSE（主路径）**
   - `GET /sse`：生成 `sessionId`（UUID），首事件 `endpoint` 下发 `/message?sessionId=xxx`，随后保持 SSE 流，推送响应与工具变更通知；30 秒心跳。
   - `POST /message?sessionId=xxx`：接收 JSON-RPC，处理后将响应经 `response_tx` 广播到对应 SSE 流，HTTP 立即返回 `202 Accepted`。
2. **POST /mcp（fallback）**：非 SSE 模式，直接返回 JSON 响应体。

### 7.3 日志系统（`LogSystem`）

- `broadcast` 通道 + 环形缓冲（上限 500 条 `LogEntry`）。
- 既供服务器内部观测，也经 Dashboard `/api/logs/stream` 向 WebUI 实时推送。

---

## 8. Web 管理面板（Dashboard）

内嵌深色主题 WebUI（`html.rs`），由 `main.rs` 挂载于根路由 `GET /`。

### 8.1 REST / SSE 路由

| 路由 | 方法 | 处理器 | 说明 |
|------|------|--------|------|
| `/` | GET | `dashboard_html()` | WebUI 首页 |
| `/api/tools` | GET | `api_tools` | 全部工具列表（含启用状态） |
| `/api/tools/:name/unload` | POST | `api_unload` | 禁用（卸载）工具 |
| `/api/tools/:name/load` | POST | `api_load` | 启用（加载）工具 |
| `/api/logs` | GET | `api_logs` | 全部历史日志 |
| `/api/logs/stream` | GET | `api_logs_stream` | 实时日志 SSE 流 |

### 8.2 能力

- 浏览器内查看全部工具及其启用状态；一键 load/unload 触发 `ToolRegistry::toggle`，并即时反映到 MCP 客户端的 `tools/list`（经 SSE list_changed）。
- 实时日志面板，订阅 `LogSystem` 环形缓冲 + 实时流。

---

## 9. 启动与运行

- 入口 `main()`（`#[tokio::main]`）：
  1. 初始化 `tracing`；
  2. 加载 `config.yaml`；
  3. 构建 `ToolRegistry` + `LogSystem`，静态注册 23 个工具；
  4. 装配 `AppState` 与三套路由 + WebUI + CORS；
  5. 绑定 `host:port`，打印启动横幅；
  6. 若 `auto_open_browser=true`，延迟 300ms 后调用系统浏览器打开 `http://host:port`；
  7. 监听 `Ctrl-C` 执行优雅关闭（`with_graceful_shutdown`）。
- 默认地址：`http://127.0.0.1:58081`。

### 9.1 已知约束

- `config.yaml` 的 `tools.discovery_path` 当前为预留字段，未启用动态发现（工具静态注册）。
- 全局 CORS 放开（任意 Origin），仅适合本机/信任网络；暴露于公网存在风险。
- `rmcp = "0.4"` 已声明却未用于协议层——协议逻辑全部手写于 `handler.rs` / `sse.rs`。

---

## 10. 总体数据流

```
客户端 ──GET /sse──▶ 建立 SSE 流（拿到 /message?sessionId）
   │
   ├─POST /message?sessionId──▶ handler::handle_request
   │                              ├─ initialize / tools/list / tools/call / ping
   │                              └─ 响应经 response_tx ─▶ SSE 流推回客户端
   │
浏览器 ──GET / ───────────────▶ 内嵌 WebUI
   ├─GET /api/tools ──────────▶ registry.list_all()
   ├─POST /api/tools/:n/load ─▶ registry.toggle() ─▶ Toggled 广播
   │                                                     └─▶ SSE 推 list_changed
   └─GET /api/logs/stream ───▶ LogSystem 实时流
```

---

## 11. MCP 2.0（2026-07-28）迁移清单

当前实现违反 2.0 规范，至少需处理以下项方可合规：

| # | 现状（2024-11-05） | 2.0 要求 | 影响文件 |
|---|--------------------|----------|----------|
| 1 | protocolVersion 硬编码 `2024-11-05` | 随 `_meta` 协商，advertise `2026-07-28` | handler.rs |
| 2 | 实现 `initialize` 握手 | 删除握手，版本/能力随请求 `_meta` | handler.rs |
| 3 | 实现 `notifications/initialized` | 同上（删除） | handler.rs |
| 4 | 实现 `ping` | 删除 `ping` | handler.rs |
| 5 | HTTP+SSE 传输（sessionId） | 改用 Streamable HTTP；移除会话模型 | sse.rs（整体移除） |
| 6 | list_changed 经 SSE 推 | 改用 `subscriptions/listen`（长连接 POST 响应流、按类型订阅） | sse.rs / handler.rs |
| 7 | 无 `server/discover` | MUST 实现 `server/discover` | handler.rs |
| 8 | 无每请求 `_meta` | 请求带 protocolVersion/clientCapabilities，响应带 serverInfo | handler.rs |
| 9 | 响应无 `resultType` | 所有结果带 `resultType`（`complete`/`input_required`） | handler.rs |
| 10 | 日志经 SSE 推流 | 废弃 `logging/setLevel`；日志级别改每请求 `_meta` | sse.rs / LogSystem |
| 11 | 无 `subscriptions/listen` / MRTR | 实现订阅长流；elicitation 改 MRTR | handler.rs |

### 11.1 建议路线

- **优先方案**：协议层改由 `rmcp = "0.4"` 官方服务端接管（其 Server/transport 应已跟随新版规范），删除手写 `sse.rs`，仅保留业务层（`ToolRegistry` / `tools/` / `dashboard/`）；
- **保留不变**：工具注册中心、热插拔 toggle、Dashboard、WebUI、LogSystem 均为 2.0 无关的业务能力，可整体复用；
- **变更集中点**：`src/mcp/*` 三件套须按 2.0 重写（传输、握手、协商、订阅、resultType、server/discover）。

---

## 12. 文档状态与待补

- ✅ 已记录：架构、模块、配置、注册中心、工具集、传输层、Dashboard、启动流程。
- ⬜ 待补（如需深化）：每个工具的输入/输出 Schema 明细；`tools/call` 的执行与错误码路径；`executor` trait 的具体签名；`html.rs` 的 UI 组件清单。
- ⬜ 待决议：是否启用 `discovery_path` 动态发现；公网暴露时的 CORS/鉴权策略。

---
*本文件为初步技术规格，随 `New_Architecture_v00` 实现演进维护。*
