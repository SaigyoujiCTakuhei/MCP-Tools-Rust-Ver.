# KazeMiMiRin MCP Server (Rust) — 技术规格文档

> 修订：2026-09-05（随实现滚动更新；取代 2026-07-22 初步版与 2026-08-30 过时横幅所指的全部旧内容）
> 状态：与当前实现一致（工具 32 · 提示词 36 · 资源 2）
> 用法定位：本文是**面向维护者的内部规格**（架构/契约/子系统）；使用说明见 README.md；历史审查见《MCP_2.0_合规与双端兼容审查报告_2026-08-30.md》；设计方案见《长期记忆功能方案.md》
> 作者：风见血月

---

## 1. 概述与设计目标

`KazeMiMiRin` MCP 服务器的 Rust 实现，双协议面 + 子进程插件架构：

- **双时代协议**：现代 MCP 2026-07-28（`POST /mcp`，每请求 `_meta`）+ Legacy 2024-11-05 HTTP+SSE（`GET /sse` + `POST /message`，为 llama.cpp UI 保留）；
- **子进程插件**：每个工具是独立可执行文件（`kzm-*`），崩溃隔离、免 DLL 锁、按二进制精确热重载，不重启服务器；
- **文件驱动的内容面**：提示词与资源放 `mcp_data/` 即生效，热重载；
- **长期记忆**：PostgreSQL + pgvector，本地嵌入，任务级进展可见；
- **开发态自动化**：源码监听器（保存即自动编译并热装载）+ 扫描新插件自愈。

非目标：多用户/权限体系、自动记忆提炼（P3）、跨机同步。

---

## 2. 系统架构

### 2.1 Cargo workspace

```
New_Architecture_v00/            # workspace 根（虚拟清单）
├── server/          kazemimirin-mcp-server（服务器本体，axum）
│   └── src/{main,config,watcher}.rs + mcp/{handler,transport,plugins}.rs
│       + registry/{tool_definition,tool_registry,prompts}.rs
│       + dashboard/{api,html}.rs + tasks.rs 位置见 mcp 同级
├── tool_kit/        插件契约：ToolDecl / ToolOutput / ToolAnnotations / kzm_tool! 宏
├── tools/           23 个通用工具（每工具一个 [[bin]]）
└── plugins/         域插件（按功能一域一 crate）
    ├── pdf_reader/           kzm-pdf-read-{local,url}
    ├── sequential_thinking/  kzm-sequentialthinking（状态外置 sessionId）
    └── memory/               kzm-memory-{remember,recall,list,forget}、
                              kzm-memory-import、kzm-db-guide、memory-daemon
```

> 注意：`cargo server`（= `run -p kazemimirin-mcp-server`）只编译服务器包。新克隆/新机器首次启动时若发现目录中无任何 `kzm-*` 产物且 `tools.watch` 开启，服务器会自动执行 `cargo build --workspace` 自愈（见 §9）。

### 2.2 共享状态（`AppState`）

| 字段 | 类型 | 职责 |
|------|------|------|
| `registry` | `Arc<ToolRegistry>` | 工具注册中心（DashMap + 变更广播） |
| `logs` | `Arc<LogSystem>` | WebUI 日志缓冲（500 条环形）+ 实时 SSE |
| `tool_timeout` | `Duration` | 单次调用超时（config，300s） |
| `auth_token` | `Option<String>` | 可选 Bearer 鉴权（`MCP_AUTH_TOKEN` > config） |
| `allowed_origins` | `Vec<String>` | Origin 白名单（回环始终放行） |
| `prompts` / `resources` | `Arc<…Registry>` | 提示词/资源注册表（文件驱动热重载） |
| `lists_changed` | `broadcast::Sender<String>` | prompts/resources 变更事件 |
| `legacy_sessions` | `Arc<LegacySessions>` | Legacy 会话表（响应路由回 SSE 流） |
| `shutdown` | `watch::Sender<bool>` | 优雅关闭信号（WebUI 按钮） |
| `discovery_dirs` | `Vec<PathBuf>` | 插件扫描目录 |
| `tasks` | `Arc<TaskRegistry>` | 运行任务注册表与进展事件流 |

### 2.3 关闭流程（单监听者模型）

```
Ctrl+C / SIGTERM / WebUI ⏻ 按钮（watch 通道）──┐
                                              ▼
              shutdown_then_deadline：打一次 🛑 日志 → grace 通知 axum 排水
                                              ▼
              排水完成 → 👋 优雅退出 ｜ 10 秒截止 → ⚠️ 强制退出（SSE 推送告警）
```

退出时卸载源码监听并写日志。WebUI ⏻ 按钮在发请求后主动释放自身 SSE 并显示全页告别屏（浏览器禁止关闭用户自开的标签页，`window.close()` 仅尽力而为）。

---

## 3. 插件契约（tool_kit）

### 3.1 进程间协议

```
<bin> decl   → stdout 输出 ToolDecl JSON（含 name/title/category/annotations/inputSchema）
<bin> call   → stdin 读 JSON 参数 → stdout 输出 ToolOutput JSON（success/data/error）
```

### 3.2 发现规则（`plugins.rs::find_plugin_binaries`）

- 扫描目录：exe 同目录（cargo 下即 `target/debug`）+ `tools.discovery_path`；
- 文件名以 `kzm-` 开头；Unix 需可执行位，Windows 需 `.exe` 后缀；
- 语言无关：编译产物与脚本插件（Shell/Python，实现了 decl/call 契约）均可。

### 3.3 侧车定义文件

`<bin名>.decl.json` 放二进制旁可覆盖内置定义（改标题/描述/参数 schema 免重编译），服务器「⟳ 重载」后生效；删除即回退内置定义。

### 3.4 热重载语义

- WebUI「⟳ 重载」（`restore_state=false`）：重探测 → 覆盖登记 → **恢复启用**；
- 源码监听自动装载（`restore_state=true`）：**保留既有禁用状态**——不悄悄重新启用用户手动卸载的工具。

---

## 4. 双时代协议面

### 4.1 现代（2026-07-28，`POST /mcp`）

- 每请求 `_meta` 必含 `io.modelcontextprotocol/protocolVersion` 与 `clientCapabilities`；
- 必需头：`MCP-Protocol-Version`（= 2026-07-28，与 `_meta` 一致）、`Mcp-Method`（tools/call 另需 `Mcp-Name`）；
- 方法：`server/discover`、`tools/list|call`、`prompts/list|get`、`resources/list|read`、`subscriptions/listen`（SSE）；
- 能力声明：tools + prompts + resources（server/discover 与 legacy initialize 一致，不虚报）；
- `category` 为 Dashboard 专用概念，**不进协议面**（tools/list 与 prompts/list 均剥离）。

### 4.2 Legacy（2024-11-05，llama.cpp UI）

- `GET /sse`：首条 `endpoint` 事件下发回传地址；会话表登记响应回传通道；
- `POST /message?sessionId=xxx`：提交即回 **202**，JSON-RPC 响应经会话流以 `message` 事件送回（官方 SDK 语义；未知会话 404）；
- `initialize` 声明 tools + prompts + resources 三项能力（§修复记录：缺失曾致 UI 面板空）。

### 4.3 错误码（已实现）

| 码 | 场景 |
|----|------|
| -32600/-32601/-32602/-32700 | JSON-RPC 标准错误 |
| -32020 | 镜像头缺失/与 body 不一致 |
| -32022 | 协议版本不受支持（附 supported 列表） |
| result.isError | 工具执行失败/校验失败/溢出（非协议错误，模型可自纠） |

---

## 5. 提示词与资源子系统（`registry/prompts.rs`）

- **提示词**：`mcp_data/prompts/*.json`（PromptDecl）或 `*.md`（YAML front matter：name/title/description/params|arguments，正文为模板——兼容 v11 Anthropic Skills 格式）；
- **渲染约定**（`render_template`）：`{{arg}}`（MCP）与 `{arg}`（v11 Python format 惯例）双支持；`{{user}}`/`{user}` 为 player_name 的嵌套别名；player_name 空值回退「你」；空串参数视为未提供；
- **资源**：`mcp_data/resources/*.json`（text 内联或 file 相对路径，file 型每次读取最新内容）；
- 热重载：`POST /api/{prompts,resources}/reload`，并经订阅流推送 `notifications/{prompts,resources}/list_changed`。

---

## 6. 长期记忆子系统（`plugins/memory`）

- **存储**：PostgreSQL 库 `Agent_Memories`（原 dsh_memory，2026-08-31 更名）表 `memory_chunks`（content + embedding vector(512)，HNSW 余弦索引）——**与 dsh 记忆插件共用的共享表，禁按 source 批量 DELETE**；
- **嵌入**：fastembed user-defined 模式加载本地 bge-small-zh-v1.5（CLS 池化，512 维），与存量向量同文余弦 1.0；
- **常驻服务** `memory-daemon`：UDS + NDJSON；批量 >5 条自动孵化常驻（模型只加载一次），≤5 条冷启动（已在则复用），空闲 `KZM_MEMORY_DAEMON_IDLE_SECS`（缺省 600s）自卸载；不可用自动回退冷启动；
- **工具**：`memory_remember`（写入）、`memory_recall`（语义检索）、`memory_list`（浏览）、`memory_forget`（删除）、`memory_import`（批量导入，增量幂等）、`db_guide`（库级内省指南，泛用不限于记忆）；
- **DSN**：`postgresql:///Agent_Memories?host=/var/run/postgresql`（peer 认证免密），`KZM_MEMORY_DSN` 可覆盖。

---

## 7. 源码监听器（`watcher.rs`，tools.watch 开启时挂载）

- 按成员隔离 mtime 快照轮询 `src` 树（500ms）+ 800ms 防抖；
- 变更成员 → `cargo build -p <成员>`（增量）→ 成功则重探测并热装载该成员全部 kzm-* 产物（**保留既有禁用状态**）；失败 → ERROR 日志附 stderr 尾部，旧版本继续服务；
- 启动自愈：发现目录无任何产物且 watch 开启 → 先全量编译再发现（新克隆场景）；
- 退出：随服务器停止并写日志「👀 源码监听已停止」，子进程 kill_on_drop 不残留。

---

## 8. 安全模型

| 层 | 机制 |
|----|------|
| 网络 | 默认绑定 127.0.0.1；Origin 回环/白名单校验（403） |
| 鉴权 | 可选 Bearer Token（覆盖 MCP 三端点；Dashboard API 未纳入，见审查报告遗留项） |
| 输入 | inputSchema 校验（失败 → isError:true） |
| 执行 | 超时 300s 连子进程终止；插件崩溃只影响单次调用 |
| CORS | 白名单 = 回环同端口 + `allowed_origins` |

---

## 9. 平台与部署

- Windows / Linux 双端；reqwest 走 rustls（无 OpenSSL）；Windows PowerShell 前置 UTF-8 输出编码，Unix 侧 bash 缺失回退 `/bin/sh`；
- `memory-daemon` 与插件均为 kill_on_drop 子进程，不残留孤儿；
- 首次/新机器：`cargo build`（全量）→ `cargo server`（自愈逻辑亦会兜底）；日常 `cargo server` 即可；
- 发布部署建议：`tools.watch: false`、仓库转私有或移除过程档案（见 .gitignore 过程档案段）。

---

## 10. 与 v11 / dsh 的关系

| 项 | 状态 |
|----|------|
| v11 的 36 个提示词 | ✅ 已迁移（.md 直接兼容，7 个 v11 存根带占位正文） |
| v11 工具（基础集/思维/记忆） | ✅ 已移植为插件 |
| memory_chunks 表 | ⚠️ 与 dsh 记忆插件**共用**（源自 HY3 会话），禁批量删 |
| python_eval / evolution / create_tool | ➖ 不移植（编译期注册已替代其框架职责） |
| ai_bridge / netease / fanqie | ⏳ 按需以 `plugins/<域>/` 移植 |
